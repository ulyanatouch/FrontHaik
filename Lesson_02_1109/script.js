// 1) вывести числа от 0 до 10 используя цикл

for (let i = 0; i <= 10; i++) {
    console.log(i);
}

// 2) вывести числа от 10 до 0 используя цикл

for (let i = 10; i >= 0; i--) {
    console.log(i);
}

// 3) вывести четные числа от 0 до 10

for (let i = 0; i <= 10; i++) {
    if (i % 2 === 0) {
        console.log(i);
    }
}

// const lst = [12, 4, 6, 3, 5, 2, 6, 32, 67, 3, 2];

// вывести все значения массива в консоль

const lst = [12, 4, 6, 3, 5, 2, 6, 32, 67, 3, 2];

for (let i = 0; i < lst.length; i++) {
    console.log(lst[i]);
}

// Этот код начинает с нулевого элемента массива lst и поочередно переходит к следующим элементам до тех пор, пока не достигнет конца массива. На каждом шаге он выводит значение элемента в консоль с помощью console.log(). Таким образом, вы увидите все значения массива в консоли.

// преобразовать все значения массива увеличив их на 10
// после преобразования вывести в консоль массив

const lst = [12, 4, 6, 3, 5, 2, 6, 32, 67, 3, 2];

// Преобразуем все значения, увеличив их на 10
for (let i = 0; i < lst.length; i++) {
    lst[i] += 10;
}

// Выводим преобразованный массив в консоль
console.log(lst);

//Этот код перебирает все элементы массива lst с помощью цикла for и увеличивает каждое значение на 10, используя оператор +=. Затем он выводит преобразованный массив в консоль с помощью console.log(). Вы увидите массив, в котором все значения увеличены на 10.

// преобразовать все значения массива заменив отрицательные числа на положительные
// ПРИМЕР: -5 -> 5

const lst = [12, -4, 6, -3, 5, 2, -6, 32, 67, -3, 2];

// Преобразуем отрицательные значения в положительные
for (let i = 0; i < lst.length; i++) {
    if (lst[i] < 0) {
        lst[i] = Math.abs(lst[i]);
    }
}

// Выводим преобразованный массив в консоль
console.log(lst);

//Этот код проходит через каждый элемент массива lst с помощью цикла for, и если элемент отрицательный (меньше нуля), то он использует функцию Math.abs() для преобразования его в положительное число. Затем он выводит преобразованный массив в консоль с помощью console.log(). Вы увидите массив, в котором все отрицательные числа заменены на положительные значения.

// second variant

const lst = [12, -4, 6, -3, 5,- 6, -7, 3, -1];
for(let i = 0; i < lst.length; i++) {
    if(lst[i] < 0 ){
        lst[i] *= (-1); // отрицательные стали положительными
    }
    console.log(lst[i]);
}


//написать цикл, который находит сумму всех чисел массива после работы цикла выведите получившуюся сумму

const lst = [12, 4, 6, 3, 5, 2, 6, 32, 67, 3, 2];
let sum = 0; // Инициализируем переменную для суммы

// Проходим через каждый элемент массива и добавляем его к сумме
for (let i = 0; i < lst.length; i++) {
    sum += lst[i];
}

// Выводим сумму в консоль
console.log("Сумма всех чисел в массиве: " + sum);

//Этот код начинает с инициализации переменной sum в ноль. Затем он проходит через каждый элемент массива lst с помощью цикла for и добавляет его значение к сумме. Наконец, он выводит полученную сумму в консоль с помощью console.log(). Вы увидите сумму всех чисел в массиве.

let summa = 0;
   const lst = [12, 4, -6, 3, 5, 2, 6, -32, -67, 3, 2];
   for (let i = 0; i <= lst.length; i++){
   summa += lst[i];
   }
   console.log(summa);